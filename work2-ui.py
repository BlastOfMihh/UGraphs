from Graph import *
from ui import *


class UiFunctions:
    def

def start():
    ui=UiFunctions()
    menu=Menu()
    # menu.add_entry(ui, "Exit", UIFunctions)
    # menu.add_entry(ui, "Read graph from file")
